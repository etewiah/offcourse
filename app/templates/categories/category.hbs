
{{#unless isTopicView}}
  <div class="selectable-topics">

Select topics below <button {{action 'saveTopicsOffline'}} > and click here to save offline </button>

{{multiselect-checkboxes options=model.topic_list.topics labelProperty='title' selection=selectedTopics }}


{{!-- 		<ul>
		{{#each item in model.topic_list.topics }}
		<li>
			{{#link-to 'categories.category.topic' item.id }}
			  {{ item.title }}
			{{/link-to}}

		</li>
		{{/each}}

		</ul> --}}

	</div>
{{/unless}}

<div>
	{{outlet}}
</div>