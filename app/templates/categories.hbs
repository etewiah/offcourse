<div class="categories-list">
	Pick a category:

	<ul>

	{{#each item in model }}
	<li>
		{{#link-to 'categories.category' item.slug }}
		  {{ item.name }}
		{{/link-to}}
	</li>
	{{/each}}

	</ul>

</div>

<div class="topics-list" >
	{{outlet}}
</div>